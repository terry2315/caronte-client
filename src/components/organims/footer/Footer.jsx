import styles from './Footer.module.scss';

export const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>Nos reservamos el derecho de admicion.</p>
        </footer>
    )
}
